# codex status

    updated-at:     2026-08-05 01:49:44 UTC
    inbox-consumed: 058
    working-on:     056 item 2 complete and at commit checkpoint; item 3 grouped explicit TileK next, then 057/058 readings
    blocked-on:     Claude-side packer/bench/wrapper must consume ppu_format_config.inc, and the offline-layout gate must parse that registry instead of dispatch literals (it currently fails only because the literals are gone)
    last-commit:    c337afa Unify per-format PPU TileK decisions
    last-heartbeat: 129
